//! A polygon or a run of segments as the editor draws it.
Shape {
    antialiasing: true
    ShapePath {
        strokeColor: "{{STROKE}}"
        strokeWidth: 1.5
        fillColor: "{{FILL}}"
        startX: {{START_X}}
        startY: {{START_Y}}
        {{PATH}}
    }
}
