//! An outline has no area and so no mass; Box2D would leave a dynamic body made
//! of one frozen in place, so the editor does not build it either.
// {{NAME}} is an outline, which has no mass, so a dynamic body cannot be made of it.
